package lib

func Init() {
	// // Initialize the map inside an init function
	// app.genericMessage = make(map[string]string)
	// app.genericMessage["joined"] = "I have joined the chat."
	// app.genericMessage["welcome"] = "Welcome to chatroom."
	// genericMessage["welcomeBack"] = "Welcome back!"
}
